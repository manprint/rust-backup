# S3-compatible module

`s3` backs up one bucket (optionally below a prefix) from AWS S3, MinIO, or a
compatible endpoint. Objects are streamed with `GetObject`; restores use bounded
5 MiB multipart parts and never create a local staging object.

Required parameter: `bucket`. Optional parameters: `endpoint`, `region`,
`prefix`, `access_key`, `secret_key`, and `path_style` (normally true for
MinIO). Destination policy is explicit: `create_bucket=true` permits a missing
bucket and `overwrite=true` permits collisions.

The plan records keys, sizes, ETags, content types and user metadata. Bucket
policy is restored when source credentials can read it. Zero-byte objects and
Unicode/`+`/`%` keys are supported. Multipart sizing grows automatically so large
objects stay below S3's 10,000-part limit.

Before any bytes are read, source analysis rejects versioned buckets, tagged
objects, non-`STANDARD` storage classes, and objects over 5 TiB: this build does
not silently drop those features. ACLs and SSE headers are not reproduced and
must be audited before production use on a provider that relies on them.

## Optional AWS smoke

Set `RUST_BACKUP_AWS_E2E=1` plus two disposable bucket names and normal AWS SDK
credentials, then run `bash e2e/s3_minio_test.sh`. The script remains MinIO-only
unless that explicit credential gate is set; never pass AWS secrets on argv.
